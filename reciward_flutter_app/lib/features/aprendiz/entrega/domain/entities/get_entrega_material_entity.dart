class GetEntregaMaterialEntity {
  final String ? nombreMaterial;
  final int ? numeroPuntos;
  final int ? numeroMaterial;

  const GetEntregaMaterialEntity({
    this.nombreMaterial,
    this.numeroPuntos,
    this.numeroMaterial
  }); 
}