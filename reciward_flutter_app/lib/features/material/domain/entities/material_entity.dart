class MaterialEntity {
  final String ? id;
  final String ? nombreMaterial;
  final int ? numeroPuntos;
  final String ? clasificacion;


  const MaterialEntity({this.id, this.clasificacion, this.nombreMaterial, this.numeroPuntos});
}